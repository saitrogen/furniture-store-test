// ============================================================
// CONFIG — change only this file when cloning for a new client
// ============================================================
export const CONFIG = {
  REPO_OWNER:       'saitrogen',
  REPO_NAME:        'furniture-store-test',
  STORE_NAME:       'Furniture Store',
  STORE_TAGLINE:    'Quality furniture — Browse & connect directly',
  DEFAULT_WHATSAPP: '919876543210',
}

CONFIG.RAW_BASE = `https://raw.githubusercontent.com/${CONFIG.REPO_OWNER}/${CONFIG.REPO_NAME}/main/`
CONFIG.API_BASE = `https://api.github.com/repos/${CONFIG.REPO_OWNER}/${CONFIG.REPO_NAME}/contents`
